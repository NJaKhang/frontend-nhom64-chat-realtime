import {Box} from "@mui/material";
import React from 'react';

const RoomList = () => {
    return (
        <Box sx={{
            gridArea: "room-list",
            width: "260px",

        }}>

        </Box>
    );
};

export default RoomList;

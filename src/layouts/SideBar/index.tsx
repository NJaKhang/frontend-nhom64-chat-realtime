import {Box} from "@mui/material";
import React from 'react';

const SideBar = () => {
    return (
        <Box
            sx={{
                gridArea: "sidebar",
                width: "72px",
                backgroundColor: "#123456"
            }}>
            
        </Box>
    );
};

export default SideBar;
